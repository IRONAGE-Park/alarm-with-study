import styled from "@emotion/styled";
// React module

/** Tooltip을 감싸고 있는 스타일 컴포넌트 */
export const StyleInfoTooltipTitleWrapper = styled.div`
  padding: 10px;
  font-size: 14px;
`;

export const StyleInfoTooltipTitleSpan = styled.span`
  display: inline-flex;
  align-items: center;
  justify-content: center;
`;
