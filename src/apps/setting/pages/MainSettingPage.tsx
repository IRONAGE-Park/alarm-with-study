import styled from "@emotion/styled";
import MainSettingContainer from "@renderer/setting/containers/MainSettingContainer";

const MainSettingPage = () => {
  return (
    <StylePage>
      <MainSettingContainer />
    </StylePage>
  );
};

const StylePage = styled.main``;

export default MainSettingPage;
